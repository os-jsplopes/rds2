name: Pull Request

on:
  push:
    branches: [hotfix/*]

jobs:
  create:
    runs-on: ubuntu-latest
    name: Create or Update

    steps:
      - uses: actions/checkout@v1
      - name: pull-request
        uses: repo-sync/pull-request@v2
        with:
          pr_title: "Pulling ${{ github.ref:11 }} into master"
          pr_assignee: ${{github.actor}}
          pr_label: "WIP,bugfix"
          pr_draft: true
          github_token: ${{ secrets.GAT }}
